{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        
    
        <button type="submit" class="btn btn-primary">Zapisz</button>
    
    {{ form_end(form) }}
{% endblock %}